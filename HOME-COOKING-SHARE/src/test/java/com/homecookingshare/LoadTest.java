package com.homecookingshare;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class LoadTest {
	
	@Test
	void load() {
//		ChangePassword command = new ChangePassword("password", "passwordsdas");
//		memberService.changePassword(mock(Validator.class), new Email("wodyd202@naver.com"), command);
	}
}
